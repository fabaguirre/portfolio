---
import { PAGE_SECTIONS, PROFILE_DATA } from "@config/constants";

const currentYear = new Date().getFullYear();
const {
  names: { socialNickname },
} = PROFILE_DATA;
---

<footer
  class="container mx-auto mb-10 mt-16 flex flex-col items-center justify-center gap-3 py-4 text-sm opacity-80 md:max-w-2xl md:flex-row md:justify-between lg:max-w-4xl"
>
  <span class="text-zinc-800/90 dark:text-zinc-200/90">
    © {currentYear}
    <a href="https://fabos.dev/" class="hover:underline" target="_blank">
      {socialNickname}
    </a>. Have a nice day! 🎉
  </span>

  <ul
    class="flex flex-col gap-1.5 font-medium md:flex-row md:gap-5 dark:text-white/90"
  >
    {
      PAGE_SECTIONS.map((link) => (
        <li>
          <a href={`#${link.id}`} class="hover:underline">
            {link.title}
          </a>
        </li>
      ))
    }
  </ul>
</footer>
