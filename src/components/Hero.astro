---
import Badge from "@components/Badge.astro";
import SocialPill from "@components/SocialPill.astro";

import GitHub from "@icons/GitHub.astro";
import LinkedIn from "@icons/LinkedIn.astro";

import { PROFILE_DATA } from "@config/constants";
import { useTranslation } from "@/i18n";

const { currentLocale } = Astro;

const { t } = useTranslation(currentLocale);

const {
  socialMedia: {
    github,
    linkedin: { url: linkedinUrl },
  },
} = PROFILE_DATA;
---

<div class="max-w-xl">
  <div class="flex gap-4">
    <img
      class="size-16 rounded-full shadow-lg"
      src="/fab.webp"
      alt={t("hero.alt")}
    />
    <a
      href={linkedinUrl}
      target="_blank"
      rel="noopener noreferrer"
      class="flex items-center"
    >
      <Badge>
        {t("hero.badge")}
      </Badge>
    </a>
  </div>

  <h1
    class="mt-4 text-4xl font-bold tracking-tight text-gray-800 sm:text-5xl dark:text-white"
  >
    {t("hero.title")}
  </h1>

  <p
    class="mt-6 text-pretty text-xl text-gray-800 dark:text-gray-300 [&>strong]:font-semibold [&>strong]:text-pink-500 dark:[&>strong]:text-pink-200"
    set:html={t("hero.description")}
  />

  <nav class="mt-8 flex flex-wrap gap-4">
    <SocialPill href={github}>
      <GitHub class="size-4" />
      GitHub
    </SocialPill>

    <SocialPill href={linkedinUrl}>
      <LinkedIn class="size-4" />
      LinkedIn
    </SocialPill>
  </nav>
</div>
